<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.AiBillOptimizer" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Starting Splash Screen Theme -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/dark_obsidian</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.AiBillOptimizer</item>
    </style>
</resources>